pipeline {
    agent any

    stages {
        stage('CleanUp') {
            steps {
                 cleanWs()
            }
        }

        stage('build') {
            steps {
                echo 'Startiong build'
                sh '/usr/local/go/bin/go build main.go'
            }
        }
    }
}