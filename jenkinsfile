pipeline {
    agent any

    stages {


        stage('build') {
            steps {
                echo 'Startiong build'
                sh '/usr/local/go/bin/go build main.go'
            }
        }
    }
}