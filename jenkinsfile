pipeline {
    agent any

    stages {
        stage('CleanUp') {
            steps {
                 cleanWs()
            }
        }

        stage('build') {
            steps {
                echo 'Startiong build'
                sh 'go build main.go'
            }
        }
    }
}